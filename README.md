A sats to fiat converter for Africans.

# Roadmap:

- User settings to configure fiat currencies they want to see
- Rearrange order of currencies
- "copy value to clipboard" buttons

# Development

- `npm install`
- `cp env.template env.local`
- set env vars accordingly
- `npm run dev`
